'use client';

// Dark mode toggle is disabled - app is permanently in dark mode
// This component is kept for future use when theme switching is re-enabled

export function DarkModeToggle() {
  // Return null since we're always in dark mode now
  return null;
}
